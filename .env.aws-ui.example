# Copy to `.env.aws-ui.local` (gitignored) for `yarn dev:ui`.
# Apollo Router on AWS (ALB DNS from CloudFormation output AlbUrl / AlbDnsName).

ROMAIN_RETREAT_SERVER_URL=http://romain-retreat-router-alb-xxxxxxx.us-east-1.elb.amazonaws.com
ROMAIN_RETREAT_SERVER_GRAPHQL_PATH=/
